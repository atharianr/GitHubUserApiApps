<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nomadev.consumerapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.nomadev.aplikasigithubuser_submission2.READ_DATABASE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_github_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_github_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiGithubUserSubmission2.NoActionBar">
        <activity
            android:name=".ui.favorite.FavoriteActivity"
            android:exported="true"
            android:theme="@style/Theme.AplikasiGithubUserSubmission2" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <queries>
        <package android:name="com.nomadev.aplikasigithubuser_submission2" />
    </queries>

</manifest>